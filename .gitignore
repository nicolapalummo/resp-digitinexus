# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets (use .env.example as template; never commit real keys)
.env
.env.*
!.env.example

# Raw AI cover output (cached source PNGs — never committed)
public/blog/covers/_raw/

# Font source files (raw TTF, ~21MB). The optimized woff2 actually served is in public/fonts/.
assets/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.dmg

# Report audit SEO (generati, non parte del sito)
digitinexus.com-audit/
